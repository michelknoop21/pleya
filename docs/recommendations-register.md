# Implementatieregister: aanbevelingen en Tautulli-defecten (DEC-132)

Aangelegd op 24 september 2026 door Michel Knoop. Spec:
`docs/superpowers/specs/2026-09-24-recommendations-design.md`. Plan:
`docs/superpowers/plans/2026-09-24-recommendations.md`.

Statussen: `OPEN`, `IN PROGRESS`, `CODE CLOSED`, `SIM CLOSED`, `LIVE ONLY`, `HARDWARE ONLY`,
`DEFERRED`. Bij elke sluiting komen de SHA en de bewijsregel erbij. Een rij verdwijnt alleen door
een eindstatus.

## Taken

| ID | Werkitem | Status | SHA / bewijs |
|----|----------|--------|--------------|
| REC-1 | DEC-132, register, DEC-062-addendum | CODE CLOSED | addee012 (DEC-132 en het DEC-062-addendum in `docs/DECISIONS.md`, dit register). DEC-132 kreeg bij REC-6 de zin over geleende verbindingen in beide richtingen (e2ca9398). |
| REC-2 | D1: Tautulli-client volgt de gemonitorde server op detail en in "Nu aan het kijken"; P6; P8-kop | CODE CLOSED | code in d43ca8fa, tests in 57530d97. Gedragswijziging: een oude Tautulli-koppeling zonder `machineIdentifier` op een profiel met twee beheerde servers heeft geen gemonitorde server meer, dus beide detailpagina's tonen de Plex-kijkers en geen regel "kijkt nu". Dat is de veilige kant: eerst kon zo'n koppeling kijkers van een andere titel tonen. Nagekomen bij REC-9 (54707e07): d43ca8fa liet de historie-importer `tautulli_client.dart` importeren voor `TautulliException.isAuth`, wat `tautulli_credential_boundary_test.dart` rood maakte. `TautulliException` staat nu in `tautulli_exception.dart` zonder token; de client exporteert hem. |
| REC-3 | D2 en D3: seeds uit het interactielog, capability `relatedHubs`, "Omdat je X kijkt" | CODE CLOSED | commit "feat(home): seeds voor Omdat je X keek uit het eigen kijklog". Bewijs: `affinity_engine_db_test.dart` groep `recentPositiveInteractions` (een rij per serie, alleen positief, binnen 30 dagen, nooit een ander profiel, geen uitgeschakelde importserver) en `discover_provider_test.dart` groep `seed rows from the interaction log` (titel per `completed`, geen fetch op een bron zonder `relatedHubs`, bekeken titels eruit, fallback bij een leeg log). Fixronde 1 (refactor naar `SeedRowsLoader` plus commit "fix(home): seedtitel volgt de serie..."): een serie met ongeziene afleveringen krijgt "kijkt", zes seeds parallel en op volgorde, dezelfde titel op twee servers geeft één rij, fallback ook als geen logseed oplost, tiebreaker op id, `recentSeeds` apart getest. Pleya Verify-regressie op het D2-geval volgt bij REC-9. Eindfixronde (commit "fix(home): seeds alleen van geschikte bronnen..."): `recentSeeds` filtert op de servers met `relatedHubs` vóór de limiet van zes (I2); een sleutel waarvan de nieuwste rij negatief is, seedt niet (I3); minder dan drie logseeds worden aangevuld uit `fetchRecentlyWatched` (besluit op de productvraag); dat serverpad slaat een gedeelde Jellyfin-verbinding over en valt weg als de controle faalt (I4); een elders uitgekeken film heet "gekeken"; seed-rijen vallen weg als geen bron met `relatedHubs` online is; Recent toegevoegde series telt mee in `alreadyShown`. Bewijs: `seed_rows_loader_test.dart` (nieuw, echte database), `affinity_engine_db_test.dart` groep `recentPositiveInteractions`, `discover_provider_test.dart` ("a title already in Recently Added Shows...", "seed rows are dropped when..."). |
| REC-4 | Lokaal partieel signaal bij een eindstop; importer vergelijkt gewichten | CODE CLOSED | commit "feat(smaak): partieel signaal bij een eindstop, importer vergelijkt gewichten". Bewijs: `interaction_recorder_test.dart` (een eindstop op 60 procent geeft één `partial` van 0,4 met genres; onder 50 procent, een gewone tik en een stop boven de kijkdrempel geven niets; een tweede stop binnen zes uur stapelt niet, een rij van zeven uur oud telt niet mee) en `tautulli_history_importer_test.dart` groep `cross-source window and weights` (een lokale partial slikt een voltooide Tautulli-view niet in, een lokale completed wel, een lokale partial slikt een partiële Tautulli-view in). Het signaal komt uit `PlaybackProgressTracker`, dus Plex, Jellyfin, Pleya Server en offline afspelen lopen via hetzelfde pad. Fixronde 1 (commit "fix(smaak): hervatten geeft een nieuwe eindstop, dedup volgt de scoringsscope"): na hervatten krijgt de nieuwe sessie een eigen eindstop (tracker-test "re-arms the final notification"); de zesuurscontrole gebruikt dezelfde scope als de scoring, dus een rij van een uitgeschakelde importserver of een ander profiel onderdrukt geen lokale partial. Bewust toegestaan: een partial en daarna een completed van dezelfde titel binnen zes uur geven samen 1,4; het venster houdt alleen partials onderling tegen. |
| REC-5 | Related hubs van seeds vier tot zes als kandidatenlaag | CODE CLOSED | commit "feat(smaak): related hubs van seeds vier tot zes voeden de kandidatenpool". De laag zit in `SeedRowsLoader`: de related hubs van seeds vier tot zes laden parallel met de rijen, hergebruiken de al opgehaalde seeds (geen tweede `fetchItem`) en kosten hooguit drie extra calls; een falende call kost alleen die seed. Ongeziene titels gaan als `hubItems` naar `buildRows`, niet als `excludeKeys`. Bewijs: `discover_provider_test.dart` groep `seed rows from the interaction log` ("seeds four to six feed the candidate pool", "a failing related hub for a candidate seed"). Meegenomen: de seed-identiteit bevat nu het soort, dus een film en een serie met dezelfde titel zijn twee seeds (test "a film and a series with the same title"). |
| REC-6 | Jellyfin-geschiedenisimport | CODE CLOSED | commit "feat(smaak): Jellyfin-kijkgeschiedenis als tweede adapter op het interactielog". `JellyfinHistoryImporter` leest over de eigen verbinding alleen de eigen gebruiker (`userId` van de connectie, DEC-062), pagina's van 200, hooguit vijf per sync, watermark op `LastPlayedDate` in `HistorySyncCursors` met `source = 'jellyfin'`. Bewijs: `jellyfin_history_importer_test.dart` (afgespeeld wordt `completed` met stabiel event-id, tweede sync stopt op de watermark na één pagina, aflevering telt voor de serie, hervatbaar tussen 50 en 90 procent is één `partial` van 0,4 ook na drie syncs, onder 50 niets, een lokale view binnen zes uur onderdrukt de import, ook per aflevering, eerste run leest hooguit `maxPagesFirstRun` pagina's), `recommendation_service_test.dart` groep `own-history importers` en `jellyfin_client_urls_test.dart` ("history import asks for its own user only"). Afwijkingen van het plan: de lokale match loopt op de aflevering en niet op de serie, zoals in de Tautulli-importer; de historiepagina vraagt `Genres,People,Studios` mee, anders heeft een film geen smaakkenmerken; de watermark schuift ook over ontdubbelde views; een epochcheck houdt een wisactie tegen. Fixronde 1 (commit "fix(smaak): geleende Jellyfin-verbindingen importeren niets..."): importers komen uit de eigen `ProfileConnection`-rijen, een verbinding die meer dan één profiel deelt importeert voor niemand, lener en uitlener allebei, omdat die Jellyfin-gebruiker de plays van beide draagt (DEC-132), niets tijdens het binden; een partial draagt de afspeeltijd; hooguit één sync per kwartier per profiel en server; een verdwenen serie telt als unresolvable. Fixronde 2 (commit "fix(smaak): DEC-132 noemt beide richtingen..."): een `lastSyncAt` in de toekomst (klok teruggezet) remt de sync niet; een Home-load tijdens het binden wacht begrensd op `awaitBindingSettle` en importeert daarna, in plaats van over te slaan. Tests in `jellyfin_history_importer_test.dart` groepen `play time, throttle, guards and resolution` en `ownJellyfinHistoryImporters`. Nagekomen bij REC-9 (54707e07): `profile_session_screen_test.dart` faalde op een ontbrekende `ProfileConnectionRegistry`, die de sessie sinds a9c71777 leest. `main.dart` levert hem al (regel 791); alleen de testopbouw miste hem. Eindfixronde (commit "fix(smaak): mislukte Jellyfin-sync houdt de kwartierrem aan..."): een mislukte sync schrijft `lastSyncAt` en laat de watermark staan, dus binnen het kwartier geen tweede poging (I1); een falende `fetchItem` voor een serie telt als unresolvable voor die rijen; de historiepagina's gooien bij een fout in plaats van leeg te lezen; het event-id van een partial draagt de stoptijd; een hervatbaar item zonder `LastPlayedDate` komt er niet in. De regel voor gedeelde verbindingen staat nu in `sharedJellyfinServerIds` en geldt ook voor de seed-fallback. |
| REC-7 | Acteurs- of regisseursrij met gedeelde cap | CODE CLOSED | commit "feat(home): rij Meer met acteur of Meer van regisseur bij warme smaak". Genre-, acteur- en regisseursrijen delen `kMaxAffinityRows` (2) plekken; een persoon moet boven `kPersonFeatureThreshold` (0,7) komen en bij gelijk gewicht wint het genre. Rij-ids `home.becauselike.actor.<slug>` en `home.becauselike.director.<slug>`, titel met de naam zoals de server hem spelt. Geen persoonsrij bij een koude smaak (REC-Q2, default nee). Bewijs: `personalized_rows_builder_test.dart` groep `person rows` (acteursrij met id en titel, regisseursrij, twee plekken met genre bij gelijkspel, sterke acteur boven zwak tweede genre, koude smaak zonder persoonsrij). Afwijking van het plan: de fixtures van de gelijkspel- en de acteurstest kregen eigen titels per kenmerk, omdat affiniteitsrijen geen titel herhalen en een gedeelde pool de latere rijen leeg liet. Fixronde 1 (commit "fix(home): persoonsrij met eigen id per naam en een bewijsdrempel"): een naam met niet-ASCII-tekens krijgt een FNV-1a-hash in het id, dus verbergen blijft per persoon; een persoon heeft een bewijsvloer op de ruwe telling (`kPersonMinTitles` 3, `kPersonMinTitleShare` 0,15), vectorschema naar 3; `topFeatures` sorteert gelijke gewichten op naam. Tests: groep `evidence floor`, "a starved candidate passes its slot on", "non-ASCII names get distinct ids", en in `taste_profile_test.dart` de tie-break en de bewijstelling. Eindreview I5: op een profiel met alleen Jellyfin komt geen persoonsrij, want de kandidatenpool van Jellyfin vraagt `People` niet op (zie REC-P10). De pool- en Similar-calls vragen sinds de eindfixronde wel `Genres,Studios`, dus genre-rijen kunnen er wel ontstaan (`jellyfin_client_urls_test.dart`, "the candidate pool and Similar calls ask for Genres and Studios, not People"). |
| REC-8 | i18n-controle, layoutblok-test, widgettest op de rijtitel | CODE CLOSED | commit "test(home): rijtitel van een seed-rij en het verbergblok van Omdat je X keek". i18n: `becauseYouAreWatching`, `moreWithActor` en `moreFromDirector` staan alleen in `en` en `nl` (3 treffers elk, 0 in de veertien andere). De gegenereerde Duitse klasse bevat de sleutels niet letterlijk: `_TranslationsDiscoverDe extends TranslationsDiscoverEn` erft ze, dus Duits en de rest tonen de Engelse titel via `fallback_strategy: base_locale`. Bewijs: `home_layout_provider_test.dart` ("one hidden because-you-watched id hides every seed row of that server": alle seed-rijen van een server delen `<server>:home.becauseyouwatched`, dus verbergen geldt voor ze allemaal; een pin, meteen groen) en `discover_screen_test.dart` groep `seed row title (TV)` (zes gevallen, en en nl maal een korte, een titel van 104 tekens en een Japanse titel: het raillabel is de vertaalde reden, de seed staat niet in zijn eigen rij, geen overflow, en alleen de lange titel wordt op een regel afgekapt door `TvSectionHeader`). Het koude-startpad (`_seedsFromServers`) levert de rij. De widgettest dekt alleen TV; mobiel (`mobile_media_rail.dart`) en desktop (`hub_section.dart`) tonen `hub.title` ook op een regel met ellips, zonder eigen test. Meegenomen: de NUL-byte in `taste_profile.dart` is nu de escape `\u0000`, zodat git het bestand weer als tekst ziet. |
| REC-9 | Volledige suite, `ci_checks`, drie Home-scenario's op de fixture, LIVE-ronde | IN PROGRESS | Suite en gate groen op 54707e07. Eerste volledige run op 2e378ffc: `+7133 ~10 -81: Some tests failed.`, waarvan 2 echte regressies van deze branch (zie REC-2 en REC-6, gefixt in 54707e07) en 79 goldens. De 79 goldens falen op base 3ad702d3 identiek (`flutter test test/goldens`: `+6 -79`, zelfde 79 testnamen); dat is de bekende macOS-tegen-Linux-baseline (CAT5, GOLD4), niet aangeraakt. Na de fix op 54707e07: `+7135 ~10 -79`, alleen die 79 goldens. `scripts/ci_checks.sh --with-unused`: `All checks passed.`, inclusief unused code en unused files. Geen Verify-scenario gaf PASS, om redenen die op base ook gelden (tabel hieronder), dus geen `SIM CLOSED`. LIVE en HARDWARE staan open. Na de eindfixronde (I1 tot en met I5, de productvraag, negen minors): `flutter test` geeft `+7167 ~10 -79`, weer alleen de 79 goldens; `scripts/ci_checks.sh --with-unused`: `All checks passed.` |

## Open punten buiten de taken

| ID | Punt | Status | Toelichting |
|----|------|--------|-------------|
| REC-P4 | Devicetoken verbruikt als `serverFriendlyName` of `version` na `register_device` faalt | DEFERRED | niet gemeten tegen `api2.py`; vraagt een clientfactory in `TautulliProvider.test` |
| REC-P7 | Eén `TautulliClient` per importpagina | DEFERRED | 25 clients per forward-pass, niet gevoeld |
| REC-P9 | Ziet een beheerd Plex Home-profiel `owned == true` in `isOwnerOrAdmin`? | LIVE ONLY | alleen op een echte server met een kindprofiel te toetsen |
| REC-PS1 | Pleya Server `GET /items/{id}/related` | OPEN (serverlijn) | zonder dit geen seed-rij voor Pleya Server-profielen |
| REC-PS2 | Pleya Server watch-state per gebruiker | OPEN (serverlijn) | zonder dit geen geschiedenisimport per profiel |
| REC-P10 | Persoonsrijen op een profiel met alleen Jellyfin | DEFERRED | De kandidatenpool en Similar vragen bij Jellyfin `Genres,Studios` en geen `People`: rollen per item maakten pagina's op kleine servers seconden trager (reden achter `_browseFields`). Zonder rollen haalt een acteur of regisseur nooit vier treffers. Oppakken als een meting laat zien dat `People` op de pool-calls betaalbaar is. |
| REC-P11 | Kosten per Home-load van de seed-rijen | DEFERRED | Niet gemeten. Per load, ook bij een delta-load: de seed-query, tot zes `fetchItem` en tot zes `fetchRelatedHubs`, plus één `fetchRecentlyWatched` per bron als het log minder dan drie seeds heeft (main deed die laatste bij elke load). Meten als een trage server klachten geeft. |
| REC-P12 | Seed-rijen lopen één load achter na een import | DEFERRED | Na een import met nieuwe rijen herbouwt `RecommendationRows.load` alleen de gepersonaliseerde rijen; de seed-rijen volgen bij de volgende Home-load. Een tweede seedronde per import kost weer tot twaalf calls. |
| REC-Q1 | `TvFocusRestoreHost` voor seed-rijen op TV | OPEN (productvraag) | default: nee |
| REC-Q2 | Persoonsrij bij niet-warme smaak | OPEN (productvraag) | default: nee |

## Verify en LIVE

De fixture is een Pleya Server-fake zonder related-endpoint, dus seed- en warme rijen zijn in de simulator niet te zien. De runs toetsen alleen D2: een Pleya Server-profiel houdt dezelfde Home-rijen. Bundels staan buiten de boom in `/tmp/pleya-verify/2026-09-25/`.

| Platform | Scenario of ronde | Status | Bundel |
|----------|-------------------|--------|--------|
| suite | `flutter test` op 54707e07 | GROEN, bekende goldens uitgezonderd | `+7135 ~10 -79: Some tests failed.` Alle 79 zijn goldens, dezelfde 79 testnamen als op base 3ad702d3 (diff van de gesorteerde namenlijsten is leeg). Geen niet-golden falers. |
| tvOS-sim | `tvos.home.walk-rails` (regressie D2) | OPEN (scenario rood op base) | `rec-tvos-home-walk-rails`, run op 54707e07, eigen simulator. `FAILED`: "DOWN from nav.discover ... landed on discover.hero.play ..., passing over 1 focusable: tv.search.pill at 68,145 892x98". Base 3ad702d3 geeft exact dezelfde fout (`base-3ad702d3-tvos-home-walk-rails`). Oorzaak: de zoekpil van VIS1 (a41b3643, 24 sep, op main) staat in een niet-zichtbare tab van de IndexedStack en heeft daar toch geometrie, dus het walk-orakel ziet hem tussen balk en hero. Het scenario heeft `nav.search` wel in `allow` voor de terugweg, niet `tv.search.pill` voor de heenweg. D2-meting uit `ui-tree/00-landing.json`: base en branch tonen dezelfde vier rails, Verder kijken, Recent toegevoegde series, Recent toegevoegd, Recent toegevoegd. De tweede rail is "Recent toegevoegde series"; "Aanbevolen voor jou" verschijnt niet. Snapshot `01-second-rail` bestaat niet, want de walk stopt bij de eerste hop. |
| iOS-sim | `ios.home.northstar` (regressie D2) | OPEN (scenario rood op main) | `rec-ios-home-northstar`, run op 54707e07, eigen simulator (Pleya Verify iPhone 15 Pro). `FAILED`: `wait_until timed out after 15000ms: {id: discover.hero}`. Bekende oorzaak sinds 5 sep: DEC-097 sluit films zonder releasedatum uit, het `/v1`-contract draagt geen datum, dus de fixture levert geen films en de iPhone toont zonder films geen hero-sectie. Het scenario is nooit aangepast; de fixture een datum geven verbiedt DEC-097. Geen screenshot, want de run stopt voor de eerste snapshot. |
| macOS | `discover.layout.macos` (regressie D2) | OPEN (niet gedraaid) | Niet gedraaid: de macOS-driver haalt de app bij elke screenshot naar voren met `osascript` (`macos_driver.dart`, `screenshot()`), en de sessie mocht geen venster activeren terwijl er op de Mac gewerkt werd. |
| iPhone of macOS, echt Plex-profiel met Tautulli en twee servers | seed-rijen, "Omdat je X kijkt", D1 | LIVE ONLY | Te meten: een warm Plex-profiel met Tautulli op server A en een tweede server B. Detail van een titel op server B toont de Plex-kijkgeschiedenis en geen Tautulli-kijkers; detail op server A toont de Tautulli-kijkers. Home toont "Omdat je X kijkt" voor de serie die het profiel nu volgt, "Omdat je X keek" voor een uitgekeken film, en na een paar weken kijken een rij "Meer met <acteur>" of "Meer van <regisseur>" als die persoon bij minstens drie bekeken titels hoort. |
| iPhone of macOS, Jellyfin-only profiel met een eigen login | import en warme rijen | LIVE ONLY | Te meten: een profiel met alleen een eigen Jellyfin-verbinding die geen ander profiel deelt. De eerste Home-load start de import van de eigen kijkgeschiedenis (hooguit één sync per kwartier; alleen een mislukte sync logt, als `JellyfinHistoryImporter: sync failed`). Bij de volgende Home-load tonen de seed-rijen "Omdat je X keek" op basis van die geschiedenis, en Top Picks en de genre-rijen worden warm. Een rij "Meer met <acteur>" of "Meer van <regisseur>" verschijnt op dit profiel niet: de pool vraagt bij Jellyfin geen `People` op (REC-P10). Geen rij toont titels die alleen een andere Jellyfin-gebruiker keek. |
| iPhone of macOS, profiel dat een Jellyfin-verbinding leent | geen import in beide richtingen | LIVE ONLY | Te meten: profiel B leent de Jellyfin-verbinding van profiel A. Voor B en voor A importeert Pleya niets van die verbinding; dat is stil, er komt geen logregel. Home van B toont alleen seed-rijen uit B's eigen lokale afspeellog, ook als dat log leeg is: de aanvulling uit `fetchRecentlyWatched` slaat de gedeelde verbinding over. Voor A geldt hetzelfde, dus de seed- en smaakrijen van A komen niet uit wat B kijkt. Rijen die de Jellyfin-server zelf levert, zoals Verder kijken, tonen wel de plays van beide, omdat ze dezelfde Jellyfin-gebruiker delen. |
| Apple TV | D-pad over de acteursrij en de seed-rijen | HARDWARE ONLY | Te meten: D-pad van de eerste rail naar de acteursrij en terug, focusherstel op de tegel. Zelfde tocht over een rij "Omdat je X kijkt"; REC-Q1 zegt dat die rij geen `TvFocusRestoreHost` krijgt, dus terug van een detailpagina landt de focus op de standaardplek van die rail. |
