# Alleen in gebruik bij de `vps`-profile (zie docker-compose.yml). In de
# NAS-opstelling termineert Cloudflare de TLS en is Caddy niet nodig.
ice.pleya.app {
	log {
		output stderr
		level ERROR
	}
	reverse_proxy relay:8080 {
		transport http {
			max_conns_per_host 100
		}
	}
}
